spring:
  datasource:
    url: jdbc:h2:mem:trading_execution_engine_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

trading:
  capital: 500000
  risk-percent: 1
  threshold-percent: 1

dhan:
  base-url: http://localhost
  client-id: test-client
  access-token: test-token
  order:
    product-type: INTRADAY
    validity: DAY
  forever:
    product-type: CNC
    validity: DAY

upstox:
  base-url: http://localhost
  access-token: test-token
